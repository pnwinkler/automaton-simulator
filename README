A work in progress - not yet ready for public use (it works, but it's somewhat buggy, and not feature-complete).

A deterministic finite automaton simulator with quizzing element. Allows user to create and test automata, against randomly generated difficulty-graded regular expressions, or against user-provided strings.

Requires:
Python 3.5 or newer
exrex
PyQt5

LGPL license is for Qt compliance.
